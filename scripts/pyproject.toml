[project]
name = "alex-scripts"
version = "0.1.0"
description = "Deployment, operations, and test scripts for Alex"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.40.8",
    "python-dotenv>=1.1.1",
    "requests>=2.32.0",
]
